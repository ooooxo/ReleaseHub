# =============================================
# Nginx 反向代理 — 可选手工覆盖 deploy.sh 生成的配置
# 默认路径：/etc/nginx/sites-available/release-hub
#
# deploy.sh 默认 NGINX_PREFIX=releasehub → 对外路径为 /releasehub/（非整站根）。
# 仅当部署时显式 NGINX_PREFIX= 空字符串时，才使用「整站根」location /。
# 无 default_server。若与 HomePortal 同域名：设 UNIFIED_NGINX=1，由 /etc/nginx/snippets/unified.d/*.conf 与本仓库片段共同维护。
# =============================================

server {
    listen 80;
    listen [::]:80;
    server_name releaseshub.local;

    # 主域：100M，适合 Cloudflare 橙云。
    # 断点续传（tus，UPLOAD_RESUMABLE=1，默认开）按 8MB 分片上传，单请求体远小于 100M，
    # 故大文件也可走主域橙云、无需 upload 子域；子域仅在关掉续传走旧整包上传时才需要。
    client_max_body_size 100M;
    client_body_timeout 300s;

    # 默认与 deploy.sh 一致：子路径反代（注意末尾 / 与 proxy_pass 末尾 /）
    # 必须写在可匹配上传的 location 内，避免只出现在 server 时仍被其它 include 的默认值限制为 1m
    location /releasehub/ {
        client_max_body_size 100M;
        client_body_timeout 300s;
        # 断点续传：分片直接流给 Node，不在 Nginx 缓冲（降低延迟与磁盘占用）。
        # X-Forwarded-* 须转发，tus onIncomingRequest 据此取客户端 IP（临时面配额）。
        proxy_request_buffering off;
        proxy_pass http://localhost:3721/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    # 若部署时 NGINX_PREFIX= 置空（整站根 /），改用下面块并删除或注释上一段 location /releasehub/
    # location / {
    #     proxy_pass http://localhost:3721;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection "upgrade";
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_read_timeout 300s;
    #     proxy_send_timeout 300s;
    # }
}

# ─────────────────────────────────────────────
# HTTPS：certbot 成功后，同一站点文件内通常会呈现类似结构
# （证书路径以 /etc/letsencrypt/live/<域名>/ 为准，由 certbot 自动写入）
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name releases.example.com;
#
#     ssl_certificate     /etc/letsencrypt/live/releases.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/releases.example.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 100M;
#     client_body_timeout 300s;
#
#     location /releasehub/ {
#         proxy_pass http://localhost:3721/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         proxy_send_timeout 300s;
#     }
# }
#
# HTTP 会由 certbot 改为仅做 301 跳转到 HTTPS，例如：
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name releases.example.com;
#     return 301 https://$host$request_uri;
# }
#
# 一键申请（在 server_name 已设为域名、DNS 已生效后）：
#   sudo certbot --nginx -d releases.example.com
# 子路径部署时 BASE_URL 形如：https://releases.example.com/releasehub
#
# ── 大文件上传子域（由 deploy.sh 写入 /etc/nginx/conf.d/release-hub-upload.conf）──
# Cloudflare 请将 upload 记录设为 DNS only（灰云）。主域可保持橙云。
#
# server {
#     listen 443 ssl http2;
#     server_name upload.example.com;
#     ssl_certificate     /etc/letsencrypt/live/upload.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/upload.example.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 2G;
#     client_body_timeout 600s;
#
#     location / {
#         proxy_pass http://localhost:3721;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 600s;
#         proxy_send_timeout 600s;
#     }
# }
# ─────────────────────────────────────────────
